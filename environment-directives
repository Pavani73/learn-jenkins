pipeline {
    agent { node { label 'workstation'} }

    environment {
          SSH = credentials('SSH')
          DEMO_URL = "google.com"
          }

    stages {
        stage('environment variable demo') {
            steps {
                echo 'environment credentials'
                sh 'env'
            }
        }
    }
}